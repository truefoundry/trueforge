# Shipped MCP catalog: server presets offered by
# GET /api/v1/mcp-servers/catalog for the settings UI to copy into
# PUT /api/v1/mcp-servers bodies. Discovery-only — never executed against.
mcp_servers:
  - name: linear
    url: https://mcp.linear.app/mcp
    auth:
      type: dcr
  - name: deepwiki
    url: https://mcp.deepwiki.com/mcp
