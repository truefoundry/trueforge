// This file was auto-generated by Fern from our API Definition.

import type * as TrueForge from "../index.js";

/**
 * Live status of the release-owned sandbox image (not persisted).
 */
export interface SandboxImage {
    /** Provider-internal build handle (the Daytona snapshot name). */
    buildRef: string;
    /** Where the image build stands right now. */
    buildStatus: TrueForge.SandboxImageBuildStatus;
    /** Tag of the release-owned sandbox image. */
    tag: string;
}
