// This file was auto-generated by Fern from our API Definition.

/**
 * Provider-specific build identity.
 */
export interface SandboxProviderResponseBuildMetadata {
    /** Provider build handle derived from the image digest (e.g. Daytona snapshot name). */
    buildRef: string;
    /** Full reference of the release sandbox image this build refers to. */
    imageUri: string;
}
