// This file was auto-generated by Fern from our API Definition.

import * as TrueHarnessTypes from "../../../src/api/index";
import { TrueHarness } from "../../../src/Client";
import { mockServerPool } from "../../mock-server/MockServerPool";

describe("ModelProvidersClient", () => {
    test("list", async () => {
        const server = mockServerPool.createServer();
        const client = new TrueHarness({ maxRetries: 0, baseUrl: server.baseUrl });

        const rawResponseBody = {
            data: [
                {
                    auth: { api_key: "api_key" },
                    base_url: "base_url",
                    models: [{ model_id: "model_id", name: "name", properties: {} }],
                    name: "name",
                    type: "openai",
                },
            ],
        };

        server
            .mockEndpoint()
            .get("/api/v1/settings/model-providers")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.settings.modelProviders.list();
        expect(response).toEqual({
            data: [
                {
                    auth: {
                        apiKey: "api_key",
                    },
                    baseUrl: "base_url",
                    models: [
                        {
                            modelId: "model_id",
                            name: "name",
                            properties: {},
                        },
                    ],
                    name: "name",
                    type: "openai",
                },
            ],
        });
    });

    test("upsert (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new TrueHarness({ maxRetries: 0, baseUrl: server.baseUrl });
        const rawRequestBody = {
            auth: { api_key: "api_key" },
            models: [{ model_id: "model_id", name: "name", properties: {} }],
            name: "name",
            type: "openai",
        };
        const rawResponseBody = {
            data: {
                auth: { api_key: "api_key" },
                base_url: "base_url",
                models: [{ model_id: "model_id", name: "name", properties: {} }],
                name: "name",
                type: "openai",
            },
        };

        server
            .mockEndpoint()
            .put("/api/v1/settings/model-providers")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.settings.modelProviders.upsert({
            auth: {
                apiKey: "api_key",
            },
            models: [
                {
                    modelId: "model_id",
                    name: "name",
                    properties: {},
                },
            ],
            name: "name",
            type: "openai",
        });
        expect(response).toEqual({
            data: {
                auth: {
                    apiKey: "api_key",
                },
                baseUrl: "base_url",
                models: [
                    {
                        modelId: "model_id",
                        name: "name",
                        properties: {},
                    },
                ],
                name: "name",
                type: "openai",
            },
        });
    });

    test("upsert (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new TrueHarness({ maxRetries: 0, baseUrl: server.baseUrl });
        const rawRequestBody = {
            auth: { api_key: "x" },
            models: [
                { model_id: "x", name: "xy", properties: {} },
                { model_id: "x", name: "xy", properties: {} },
            ],
            name: "xy",
            type: "openai",
        };
        const rawResponseBody = { error: { message: "message" } };

        server
            .mockEndpoint()
            .put("/api/v1/settings/model-providers")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.settings.modelProviders.upsert({
                auth: {
                    apiKey: "x",
                },
                models: [
                    {
                        modelId: "x",
                        name: "xy",
                        properties: {},
                    },
                    {
                        modelId: "x",
                        name: "xy",
                        properties: {},
                    },
                ],
                name: "xy",
                type: "openai",
            });
        }).rejects.toThrow(TrueHarnessTypes.BadRequestError);
    });

    test("catalog", async () => {
        const server = mockServerPool.createServer();
        const client = new TrueHarness({ maxRetries: 0, baseUrl: server.baseUrl });

        const rawResponseBody = {
            data: [{ models: [{ model_id: "model_id", name: "name", properties: {} }], name: "name", type: "openai" }],
        };

        server
            .mockEndpoint()
            .get("/api/v1/settings/model-providers/catalog")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.settings.modelProviders.catalog();
        expect(response).toEqual({
            data: [
                {
                    models: [
                        {
                            modelId: "model_id",
                            name: "name",
                            properties: {},
                        },
                    ],
                    name: "name",
                    type: "openai",
                },
            ],
        });
    });
});
