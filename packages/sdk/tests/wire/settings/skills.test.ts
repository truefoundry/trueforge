// This file was auto-generated by Fern from our API Definition.

import * as TrueForgeTypes from "../../../src/api/index";
import { TrueForge } from "../../../src/Client";
import { mockServerPool } from "../../mock-server/MockServerPool";

describe("SkillsClient", () => {
    test("list (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new TrueForge({ maxRetries: 0, baseUrl: server.baseUrl });

        const rawResponseBody = {
            data: [{ description: "description", name: "name", path: "path", ref: "ref", type: "git", url: "url" }],
        };

        server
            .mockEndpoint()
            .get("/api/v1/settings/skills")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.settings.skills.list();
        expect(response).toEqual({
            data: [
                {
                    description: "description",
                    name: "name",
                    path: "path",
                    ref: "ref",
                    type: "git",
                    url: "url",
                },
            ],
        });
    });

    test("list (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new TrueForge({ maxRetries: 0, baseUrl: server.baseUrl });

        const rawResponseBody = { error: { message: "message" } };

        server
            .mockEndpoint()
            .get("/api/v1/settings/skills")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.settings.skills.list();
        }).rejects.toThrow(TrueForgeTypes.UnauthorizedError);
    });

    test("list (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new TrueForge({ maxRetries: 0, baseUrl: server.baseUrl });

        const rawResponseBody = { error: { message: "message" } };

        server
            .mockEndpoint()
            .get("/api/v1/settings/skills")
            .respondWith()
            .statusCode(403)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.settings.skills.list();
        }).rejects.toThrow(TrueForgeTypes.ForbiddenError);
    });

    test("upsert (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new TrueForge({ maxRetries: 0, baseUrl: server.baseUrl });
        const rawRequestBody = { description: "description", name: "name", ref: "ref", type: "git", url: "url" };
        const rawResponseBody = {
            data: { description: "description", name: "name", path: "path", ref: "ref", type: "git", url: "url" },
        };

        server
            .mockEndpoint()
            .put("/api/v1/settings/skills")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.settings.skills.upsert({
            description: "description",
            name: "name",
            ref: "ref",
            type: "git",
            url: "url",
        });
        expect(response).toEqual({
            data: {
                description: "description",
                name: "name",
                path: "path",
                ref: "ref",
                type: "git",
                url: "url",
            },
        });
    });

    test("upsert (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new TrueForge({ maxRetries: 0, baseUrl: server.baseUrl });
        const rawRequestBody = { description: "x", name: "xy", ref: "x", type: "git", url: "x" };
        const rawResponseBody = { error: { message: "message" } };

        server
            .mockEndpoint()
            .put("/api/v1/settings/skills")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.settings.skills.upsert({
                description: "x",
                name: "xy",
                ref: "x",
                type: "git",
                url: "x",
            });
        }).rejects.toThrow(TrueForgeTypes.BadRequestError);
    });

    test("catalog (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new TrueForge({ maxRetries: 0, baseUrl: server.baseUrl });

        const rawResponseBody = {
            data: [{ description: "description", name: "name", path: "path", ref: "ref", type: "git", url: "url" }],
        };

        server
            .mockEndpoint()
            .get("/api/v1/settings/skills/catalog")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.settings.skills.catalog();
        expect(response).toEqual({
            data: [
                {
                    description: "description",
                    name: "name",
                    path: "path",
                    ref: "ref",
                    type: "git",
                    url: "url",
                },
            ],
        });
    });

    test("catalog (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new TrueForge({ maxRetries: 0, baseUrl: server.baseUrl });

        const rawResponseBody = { error: { message: "message" } };

        server
            .mockEndpoint()
            .get("/api/v1/settings/skills/catalog")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.settings.skills.catalog();
        }).rejects.toThrow(TrueForgeTypes.UnauthorizedError);
    });

    test("catalog (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new TrueForge({ maxRetries: 0, baseUrl: server.baseUrl });

        const rawResponseBody = { error: { message: "message" } };

        server
            .mockEndpoint()
            .get("/api/v1/settings/skills/catalog")
            .respondWith()
            .statusCode(403)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.settings.skills.catalog();
        }).rejects.toThrow(TrueForgeTypes.ForbiddenError);
    });
});
