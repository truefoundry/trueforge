---
title: "Custom Theme"
description: "Build your own custom theme"
---

## The theme object

Pass design tokens on the theme object to override component styles. You can also override app-level CSS classes for broader layout and styling changes.

```jsx App.jsx
type ThemeConfig = {
  ...
  tokens?: Partial<SemanticTokens>;
  icons?: IconMap;
  ...
};
```

## The token object

Override individual design tokens to change colors and other visual properties: sidebar, page background, popups, buttons, inputs, and the rest of the surfaces listed below.

Set tokens separately for light and dark mode so both schemes stay consistent without per-component CSS.

The fastest way to try tokens is the [UI Config App](https://ui.trueforge.dev/). Change colors in the preview, then copy the generated config into your app.


```json Token Object
{
  "light": {
    "sidebarBg": "#FAFAFA",
    "primaryBg": "#FFFFFF",
    "textPrimary": "#18181B",
    "primaryButtonBg": "#6366F1",
    "userMessageBg": "#6366F1"
    // ...others (see the full list below)
  },
  "dark": {
    // ...same tokens as light mode
  }
}
```

### All tokens

Default values are from the built-in `trueforge` preset (light mode). See [Token naming](#token-naming) for how each key maps to a CSS variable.

| Token name | Default value | Description |
| --- | --- | --- |
| `sidebarBg` | `#FAFAFA` | Sidebar background. |
| `topbarBg` | `#FFFFFF` | Top bar background. |
| `primaryBg` | `#FFFFFF` | Primary page background. |
| `secondaryBg` | `#F4F4F5` | Secondary surface background. |
| `border` | `#E4E4E7` | Default border color. |
| `fontFamily` | `ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, sans-serif` | Base UI font family. |
| `inputBoxBg` | `#FAFAFA` | Input field background. |
| `inputBorder` | `#E4E4E7` | Input field border color. |
| `textPrimary` | `#18181B` | Primary text color. |
| `textSecondary` | `#62626A` | Secondary / muted text color. |
| `cardBg` | `#FFFFFF` | Card surface background. |
| `dropdownSelectedItemBg` | `#F1F1F2` | Selected dropdown item background. |
| `dropdownSelectedItemText` | `#18181B` | Selected dropdown item text color. |
| `userMessageBg` | `#6366F1` | User chat message bubble background. |
| `userMessageText` | `#FFFFFF` | User chat message text color. |
| `assistantMessageBg` | `transparent` | Assistant chat message background. |
| `assistantMessageText` | `#18181B` | Assistant chat message text color. |
| `primaryButtonBg` | `#6366F1` | Primary button background. |
| `primaryButtonHover` | `#4F46E5` | Primary button hover background. |
| `primaryButtonText` | `#FFFFFF` | Primary button text color. |
| `secondaryButtonBg` | `#FAFAFA` | Secondary button background. |
| `secondaryButtonHover` | `#E4E4E7` | Secondary button hover background. |
| `secondaryButtonText` | `#18181B` | Secondary button text color. |
| `ghostButtonBg` | `transparent` | Ghost button background. |
| `ghostButtonHover` | `#F4F4F5` | Ghost button hover background. |
| `ghostButtonText` | `#18181B` | Ghost button text color. |
| `successBg` | `#16A34A` | Success state background. |
| `successText` | `#FFFFFF` | Success state text color. |
| `failureBg` | `#EF4444` | Error / failure state background. |
| `failureText` | `#FFFFFF` | Error / failure state text color. |
| `warningBg` | `#D97706` | Warning state background. |
| `warningText` | `#FFFFFF` | Warning state text color. |
| `focusRing` | `#E4E4E7` | Focus ring color. |
| `radius` | `0.5rem` | Base border radius. |
| `composerRadius` | `1.5rem` | Chat composer border radius. |
| `overlay` | `rgb(24 24 27 / 0.5)` | Modal / overlay backdrop color. |
| `shadowColor` | `#18181B` | Shadow color. |
| `scrollbarThumb` | `#62626A` | Scrollbar thumb color. |

## Token naming

SemanticTokens keys map directly to kebab-cased CSS variables. For example, `primaryButtonBg` maps to `--primary-button-bg`, and `userMessageText` maps to `--user-message-text`. The only exception is `fontFamily`, which maps to the `--font-agent-ui` CSS variable.

To apply a brand primary color and font across the SDK, override the matching semantic tokens:
```ts
const theme = {
  tokens: {
    primaryButtonBg: "#635BFF",
    primaryButtonHover: "#5147E5",
    primaryButtonText: "#FFFFFF",
    fontFamily: "Inter, sans-serif",
    composerRadius: "12px",
  },
};
```
Those tokens map to CSS variables:
```ts
--primary-button-bg
--primary-button-hover
--primary-button-text
--font-agent-ui
--composer-radius
```
Changing a few semantic tokens updates the UI consistently without writing component-specific CSS.

## Styling rendered content

`theme.classNames` (`ContentClassNames`) adds host CSS classes onto content renderers. Pass it through `theme` on `TrueforgeUI` (or `SlotsProvider`):

```tsx
<TrueforgeUI
  theme={{
    classNames: {
      markdown: "prose prose-sm",
      openui: { root: "my-openui" },
    },
  }}
  /* … */
/>
```

```ts
type ContentClassNames = {
  /** Root of the markdown renderer (joins `.aui-markdown`). */
  markdown?: string;
  /** Inline `` `code` `` spans inside markdown. */
  inlineCode?: string;
  syntaxHighlighter?: {
    root?: string; // joins `.aui-syntax-highlighter`
    pre?: string;
    code?: string;
    lineNumber?: string;
  };
  openui?: {
    root?: string; // joins `.aui-openui`
    scope?: string; // OpenUI theme scope (`.markdown-openui-scope`)
  };
  monaco?: {
    root?: string; // joins `.aui-monaco`
    editor?: string; // Monaco mount container
    /** Monaco theme name; when unset, follows light/dark mode. */
    monacoTheme?: string;
  };
};
```

For CSS-only adjustments you can instead target the stable class names directly: `.aui-markdown`, `.aui-syntax-highlighter`, `.aui-openui`, `.aui-monaco`.
