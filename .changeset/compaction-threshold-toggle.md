---
'@truefoundry/trueforge-ui': patch
---

Make the compaction threshold an optional override: off by default (no `trigger`, runtime derives the threshold), and toggling on reveals a number input defaulting to 50000.
