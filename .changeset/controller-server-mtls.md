---
"@truefoundry/trueforge": minor
---

Add optional mutual TLS for the HTTPS listener and schedule controller→server hop via `TLS_MUTUAL` / `TLS_DIR`. Off by default; independent of ServiceFoundry `TRUEFOUNDRY_MTLS_*`.
