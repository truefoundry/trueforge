---
"@truefoundry/trueforge-ui": minor
---

Open the Agent Config model picker as an inline dropdown instead of a separate modal.
Search providers and models across the full catalog with normalized matching.
