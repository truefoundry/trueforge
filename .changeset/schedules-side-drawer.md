---
"@truefoundry/trueforge-ui": minor
---

Add global Schedules page at `/schedules` with listing, popover-based filters, and create/edit drawer wired to the schedule API. Add Table primitives with client-side pagination and portal DropdownMenu so row actions are not clipped by overflow. Export a reusable popover select with single- and multi-select modes. Remove per-agent schedules from Agents Library.
