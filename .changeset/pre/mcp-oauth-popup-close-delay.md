---
"@truefoundry/trueforge-ui": patch
---

Stop the MCP OAuth opener from closing the popup as soon as the callback broadcasts, so the success/failure screen can show before the popup closes itself.
