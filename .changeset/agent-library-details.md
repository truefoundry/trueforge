---
"@truefoundry/trueforge-ui": minor
---

Add routed agent detail pages with lazy Overview, Sessions, and Use In Code tabs, plus a Harness `AgentSessionsServer` adapter (`getAgent`, `getCodeSnippets`, `listSessions`, `listSessionEvents`) wired into `createTrueForgeAgentUIServer`.
