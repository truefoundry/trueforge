---
"@truefoundry/trueforge-ui": patch
---

Add agent-filtered chat history labels, preserve Try Agent and explicit history-filter intent in URL query state while resolving backend IDs, keep Try Agent highlighted as chat, and route fresh agent builders at `/build-agent` before their session URL is assigned.
