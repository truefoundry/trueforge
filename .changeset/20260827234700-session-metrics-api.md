---
'@truefoundry/trueforge-core': patch
'@truefoundry/trueforge': patch
---

Add caller-scoped session metrics meters, chart catalog, and chart-data endpoints for named agents. Distribution meters include every session in the window (including zero-turn / zero-duration).
