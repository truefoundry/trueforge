---
'@truefoundry/trueforge-core': patch
'@truefoundry/trueforge': patch
---

Add caller-scoped session metrics meters, charts, and chart-data under `/internal/sessions` via a server-owned `ISessionMetricsStore`.
