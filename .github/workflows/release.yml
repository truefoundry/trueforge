# npm publish for the four public packages.
#
# npm trusted publishers are bound to `release.yml`.
# Do not rename this file without updating every package on npmjs.com.
#
# On every push to main, changesets/action/select-mode chooses:
#   - pending `.changeset/*.md` → version job opens/updates the Version Packages PR
#     (install only; that PR is gated by CI.yml)
#   - none pending + unpublished versions → pack (build/test) then publish
# Merge the Version Packages PR to publish. There is no separate publish Action
# and no `v*` tag trigger.
#
# Auth is GitHub OIDC (no NPM_TOKEN). An `_authToken` in user `.npmrc` disables
# pnpm's OIDC exchange, so the publish job must not set NPM_TOKEN / NODE_AUTH_TOKEN.
# id-token is granted only on the publish job (trusted-publishing best practice).
name: Release

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  # Never cancel a publish that is already talking to npm.
  cancel-in-progress: false

permissions: {}

jobs:
  select-mode:
    name: Select mode
    runs-on: ubuntu-latest
    timeout-minutes: 10
    outputs:
      mode: ${{ steps.select-mode.outputs.mode }}
      publish-plan-artifact-id: ${{ steps.select-mode.outputs.publish-plan-artifact-id }}
    permissions:
      contents: read
    steps:
      - name: Check out repo
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - uses: pnpm/action-setup@v6

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - name: Select Changesets mode
        id: select-mode
        uses: changesets/action/select-mode@v2

  version:
    name: Version packages
    if: needs.select-mode.outputs.mode == 'version'
    needs: select-mode
    runs-on: ubuntu-latest
    # Fern rebake when the sdk version moves.
    timeout-minutes: 30
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Check out repo
        uses: actions/checkout@v7
        with:
          # Full history so `changeset version` can write CHANGELOG from commits.
          fetch-depth: 0
          persist-credentials: false

      - uses: pnpm/action-setup@v6

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      # Must be @v2: @changesets/cli@3 stores applied RC changesets under
      # `.changeset/pre/`; @v1 still expects folder-style `pre/changes.md` and
      # fails with ENOENT right after the Version Packages merge.
      - name: Version packages
        id: version
        uses: changesets/action/version@v2
        with:
          script: pnpm run version
          push-with-git-cli: true

  pack:
    name: Pack packages
    if: needs.select-mode.outputs.mode == 'publish'
    needs: select-mode
    runs-on: ubuntu-latest
    timeout-minutes: 30
    outputs:
      pack-dir-artifact-id: ${{ steps.pack.outputs.pack-dir-artifact-id }}
    permissions:
      contents: read
    steps:
      - name: Check out repo
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - uses: pnpm/action-setup@v6

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 24
          package-manager-cache: false

      - run: pnpm install --frozen-lockfile

      - run: pnpm build
      - run: pnpm test

      # Root build copies FE into packages/trueforge/dist/_frontend; refuse an API-only tarball.
      - name: Verify packaged UI is present
        run: |
          test -f packages/trueforge/dist/cli.js
          test -f packages/trueforge/dist/_frontend/index.html

      - name: Pack packages
        id: pack
        uses: changesets/action/pack@v2
        with:
          publish-plan-artifact-id: ${{ needs.select-mode.outputs.publish-plan-artifact-id }}

  publish:
    name: Publish packages
    needs: pack
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: write
      id-token: write
    steps:
      - name: Check out repo
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - uses: pnpm/action-setup@v6

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 24
          # No registry-url / NODE_AUTH_TOKEN: an `_authToken` in .npmrc disables
          # pnpm's OIDC exchange for npm trusted publishing.
          package-manager-cache: false

      - run: pnpm install --frozen-lockfile

      - name: Publish packages
        uses: changesets/action/publish@v2
        with:
          pack-dir-artifact-id: ${{ needs.pack.outputs.pack-dir-artifact-id }}
          create-github-releases: true
        # TODO(oss): set NPM_CONFIG_PROVENANCE once the repo is public. Do NOT
        # set it now: provenance publicly attests the source repo and commit.
        # publishConfig.provenance is false on every public package.
